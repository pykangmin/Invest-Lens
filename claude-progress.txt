# 세션 진행 기록

## 2026-04-22 — 파이프라인 검증

- Vite + React + TypeScript 초기화 (react-ts 템플릿, Vite 8)
- ECharts, PapaParse, lodash, dayjs 설치 + tslib 보조 설치(echarts-for-react transitive dep 누락 이슈 해결)
- types → data-loader → analysis → visualization → layout 레이어 흐름으로 허수아비 라인 차트 1개 구현
- Vite 기본 App.tsx를 src/layout/App.tsx로 재작성, index.css는 src/shared/로 이동
- 로컬 빌드 성공 (npm run build, dist/ 생성), 개발 서버 정상 (http://localhost:5173/ 200 OK)
- Vercel 프로덕션 배포 성공
  - Production: https://claude-jm15jk9jl-ttdd3374-1774s-projects.vercel.app
  - Alias: https://claude-gamma-three.vercel.app
- build-test 브랜치로 push 완료

다음 세션에서 주목할 점:
- 이 구현은 모두 허수아비다. 아이디어 확정 후 types/number.ts, data-loader/mock-timeseries.ts, analysis/identity.ts, visualization/line-chart.tsx, layout/App.tsx는 실제 로직으로 교체 예정
- skills/ 하위 5개 파일은 여전히 제목과 TODO만 있는 상태 — 데이터 해석 규칙 엔진을 작성해야 함
- feature_list.json은 빈 배열 []. 기능 단위 분해 후 채워야 함
- verify-structure.sh, verify-skills.sh, init.sh는 echo TODO 수준의 스켈레톤. 실제 검증 로직 미구현
- docs/architecture/LAYERS.md와 docs/guides/SETUP.md는 상세 내용이 채워진 상태 (이번 세션 이전에 완성됨)
- ECharts 번들이 1.3MB로 커서 빌드 경고 발생. 허수아비 단계에서는 무시, 이후 code-splitting 고려

## 2026-04-22 — Invest Lens 디자인 충실 구현 (build-test)

- Tailwind v4 설치(@tailwindcss/vite 플러그인). vite.config.ts에 플러그인 등록, src/shared/index.css 상단에 @import "tailwindcss" 추가
- 허수아비 라인 차트 화면을 전면 교체. 뷰 4개(main, fundamentals, macro, technical)를 useState 문자열 state로 전환. 라우팅 라이브러리 미사용
- 공통 Header: "💎 Invest Lens" 로고 + 검색창(placeholder "오늘은 어떤 종목을 분석 해볼까요?", 기능 없음, readOnly) + 돋보기 아이콘(SVG)
- MainView: "Google 102.36$ +2.36$ (0.87%)" 상단 + S&P 500 카드 4개 / 좌측 2x2 지표 카드(기업 펀더멘털 60, Soft Landing, 빈 카드, Fear & Greed 게이지 23) + 우측 차트. 카드 hover 시 우측 차트 전환(fundamentals→트리맵, macro→오버레이, technical→이동평균, empty→빈 상태), 해제 시 마지막 상태 유지. MORE » 또는 카드 클릭 시 서브 뷰로 이동(빈 카드는 반응 없음). 관련 뉴스 4건
- FundamentalsView: BACK + 제목 + 우측 상단 빨강 스코어 뱃지 "20". 지표 2x2(S&P PER, EPS Growth, PBR, 배당수익률) + AI 인사이트 4개 + 하단 섹터 트리맵
- MacroView: 녹색 "Soft Landing" 뱃지 + 지표(Fed Rate, 10Y Yield, CPI, DXY) + 인사이트 + S&P500/FedRate/M2 상관관계 오버레이(듀얼 axis)
- TechnicalView: 파랑 "Extreme Greed" 뱃지 + 지표(VIX, RSI, MACD, 거래량) + 인사이트 + 종가/MA20/MA50/MA200 멀티 라인
- 모든 더미 데이터는 각 컴포넌트 내부 상수로 선언. data-loader/analysis 레이어는 import하지 않음(기존 허수아비 파일 삭제하지 않고 보존)
- 로컬 빌드 성공, dev 서버 HTTP 200
- 커밋 2건: chore(infra) Tailwind 설치, feat(layout) 뷰 4개 + 차트 4개 구현

다음 세션에서 주목할 점:
- 이 단계는 "디자인 구현 가능성 검증"까지. Skills 정합성/범용성/실데이터 연결은 아직 안 함
- data-loader(mock-timeseries.ts), analysis(identity.ts), types(number.ts), visualization/line-chart.tsx는 사용되지 않는 상태로 남아있음. 본격 아이디어 확정 시 제거·교체 예정
- skills/ 5개 파일은 여전히 TODO. feature_list.json은 []. verify-*.sh는 echo 스켈레톤
- ECharts 번들 1.3MB 경고 여전. 동적 import/code-splitting은 아직 도입 안 함
- Fear & Greed 게이지가 카드 안에 다소 큼직하게 들어가 있어 카드 높이 균형 확인 필요. 필요 시 dashboard-layout.skill.md에서 카드 내부 비율 규칙 정의

## 2026-04-23 — 규칙 준수 전면 재작성 (build-test)

배경: 이전 세션이 Tailwind CSS를 도입했으나 tech-stack.md 목록에 없어 규칙 위반.
이번 세션에서 사용자 지시("규칙 우선 — 옵션 a") 및 신규로 추가된
skills/frontend-aesthetics.skill.md(실제 내용 있음)에 맞춰 전면 재작성.

주요 변경:
- Tailwind 및 @tailwindcss/vite 제거. vite.config.ts에서 플러그인 삭제.
- src/shared/index.css를 frontend-aesthetics.skill.md 규칙에 맞춘 글로벌 CSS로
  전면 재작성 (약 490줄). 주요 준수 항목:
    · 폰트: Wanted Sans Variable 주, Pretendard 대안. system-ui/Noto Sans KR/
      Arial/Roboto/Inter 등 금지 폰트 미사용. index.html에서 두 폰트 CDN 링크.
    · font-variant-numeric: tabular-nums 전역 적용.
    · 카드는 옵션 A(테두리 중심). 기본 상태 box-shadow 미사용.
    · 5색 의미 체계: 상승 녹(#22c55e), 하락 빨(#ef4444), 정보 파랑(#3b82f6),
      강조 노랑(#eab308), 구조 무채색(slate 계열).
- layout 컴포넌트 전면 재작성: Tailwind 유틸 클래스 전부 제거, plain CSS 클래스로 교체.
- main.png 규격 반영: 좌하단 카드가 "빈 카드"가 아니라 "기업 펀더멘털 & 평가 / 60"
  중복으로 변경 (이미지 기준).
- 서브 뷰 차트 추가: sub1에 SectorYtdBarChart(섹터별 YTD 비교 막대), sub3에
  RsiChart(14D, 과매수 70/과매도 30 마크라인) 추가.
- 재사용 컴포넌트 분리: BackButton, IndicatorCard, InsightPanel (모두 layout 레이어 내부).
- 허수아비 잔재 삭제: src/types/number.ts, src/data-loader/mock-timeseries.ts,
  src/analysis/identity.ts, src/visualization/line-chart.tsx. .gitkeep만 유지.
- index.html: 제목 "Invest Lens"로 변경, lang="ko", 웹폰트 CDN 링크 추가.

검증 결과:
- 레이어 의존성: visualization에서 layout/analysis/data-loader import 없음. 순방향 유지.
- 금지 폰트 미사용 (Grep 확인).
- 기본 상태 box-shadow 미사용 (옵션 A 준수).
- npm run build 성공. CSS 6.48 kB (Tailwind 시절 16.66 kB에서 축소).
- dev 서버 HTTP 200.
- Vercel 프로덕션 배포 성공.

커밋/이슈:
- 실수: 파일 일괄 staging 후 git reset이 기대만큼 unstaging 안 되어, "docs(skills):"
  메시지 커밋에 구현 변경분까지 모두 포함되었음(c063b6a). 내용은 정상이나
  메시지가 범위를 완전히 설명하지 않음. 다음 세션에서 커밋 분리가 필요하면 기록.

다음 세션에서 주목할 점:
- 다른 Skills 파일 4종(data-profile, data-analysis, visualization, insight,
  dashboard-layout)은 여전히 TODO. frontend-aesthetics와 책임 경계 정리 필요
  (frontend-aesthetics.skill.md 말미 "다른 Skills와의 관계" 참고).
- verify-skills.sh는 아직 echo 스켈레톤. frontend-aesthetics.skill.md의 "금지 목록"
  섹션을 파싱해서 실제 코드에서 해당 항목(금지 폰트/그라디언트/라이브러리 등)이
  등장하는지 검사하도록 구현 필요.
- feature_list.json은 여전히 []. 전체 기능을 분해해서 채워야 함.
- ECharts 번들 1.34MB 경고 여전. 동적 import/code-splitting 미도입.
- 차트 더미 데이터는 모두 컴포넌트 내 상수. 본격 데이터 연결 시
  src/data-loader/와 src/analysis/를 활성화할 것.
